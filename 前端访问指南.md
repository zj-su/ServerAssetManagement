# 前端访问指南

**更新时间**: 2025-01-27

---

## 🌐 前端访问方式

### 1. 本地访问

如果您的浏览器和服务器在同一台机器上：

**访问地址**: http://localhost:3001

直接在浏览器中打开上述地址即可访问前端界面。

### 2. 远程访问

如果您的浏览器在其他机器上，需要使用服务器的IP地址：

**访问地址**: http://服务器IP:3001

例如：http://192.168.1.100:3001

---

## 🚀 启动前端服务

如果前端服务未运行，可以使用以下命令启动：

```bash
cd /root/idc资产管理/IDC项目/frontend
npm run start
```

或者后台运行：

```bash
cd /root/idc资产管理/IDC项目/frontend
nohup npm run start > /tmp/frontend.log 2>&1 &
```

---

## 📋 前端配置

- **开发服务器**: Vite
- **端口**: 3001
- **后端API地址**: http://localhost:8000/api/v1
- **代理配置**: 已配置API代理，前端请求 `/api` 会自动转发到后端

---

## 🔧 检查服务状态

### 检查前端服务是否运行

```bash
ps aux | grep vite | grep -v grep
```

### 检查端口是否监听

```bash
netstat -tlnp | grep 3001
# 或
ss -tlnp | grep 3001
```

### 查看前端日志

```bash
tail -f /tmp/frontend.log
```

---

## ⚠️ 注意事项

1. **防火墙**: 如果无法远程访问，请检查防火墙是否开放3001端口
   ```bash
   # CentOS/RHEL
   firewall-cmd --add-port=3001/tcp --permanent
   firewall-cmd --reload
   ```

2. **后端服务**: 确保后端服务（端口8000）也在运行
   ```bash
   ps aux | grep uvicorn | grep -v grep
   ```

3. **首次访问与登录**: 
   - 打开地址后首先看到 **登录页**（与项目介绍一致）
   - 若已执行过 `register_user.py`，可使用默认管理员：**用户名 admin，密码 pass123**
   - 若无用户，在项目根目录执行：`python3 register_user.py` 创建 admin 账户
   - 登录成功后进入 **IDC资产管理系统** 主界面

---

## 🎯 快速测试（与项目介绍 7.1 一致）

1. **访问前端**: 打开浏览器访问 http://localhost:3001
2. **登录系统**: 在登录页输入用户名和密码（支持本地用户与 AD 域用户）
3. **主界面功能**: 登录后可访问左侧菜单（**报废**、**入库** 均有一级入口）：
   - **资产管理**：资产列表、搜索、创建/编辑、状态管理、回收站
   - **报废管理**：待报废/已报废资产、确认报废、移至回收站
   - **配件管理**：配件列表与操作
   - **入库管理**（点击展开）：
     - **配件入库单**：配件入库单列表与明细
     - **服务器入库单**：服务器入库单列表与明细
   - **服务器管理**（点击展开）：服务器概览、机柜管理、电源控制
   - **用户管理**：用户列表、AD 域同步
   - **AD域配置**：AD 域连接与测试

---

## 📝 常见问题

### Q: 页面显示空白或无法加载？
A: 检查前端服务是否运行，查看日志文件 `/tmp/frontend.log`

### Q: 登录后提示API错误？
A: 检查后端服务是否运行在8000端口

### Q: 无法远程访问？
A: 检查防火墙设置和服务器IP地址

---

**前端服务配置完成！** ✅
