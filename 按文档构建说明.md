# IDC资产管理系统 - 按文档构建说明

**依据文档**: `项目介绍.md`（全面项目分析报告）  
**构建时间**: 按文档要求重新构建

---

## 一、数据库（与项目介绍 5.1 一致）

### 1. 迁移脚本 `backend/update_database_schema.py`

- **users 表**：补全 AD 域相关字段  
  - `is_ad_user`、`ad_dn`、`display_name`、`updated_at`
- **assets 表**：补全文档 5.1 字段  
  - `asset_code`、`contract_number`、`purchased_with_server_sn`  
  - `cpu_model`、`cpu_cores`、`memory_gb`、`disk_info`
- 支持数据库路径：`backend/server_management.db` 或 `backend/app/server_management.db`

### 2. 执行迁移

```bash
cd /root/idc资产管理/IDC项目/backend
python3 update_database_schema.py
```

---

## 二、后端（与项目介绍 2.1、3.x、5.1 一致）

### 1. 应用标题与配置

- **main.py**：`FastAPI(title="IDC资产管理系统")`，根路径返回「欢迎使用IDC资产管理系统API」
- **config.py**：`PROJECT_NAME = "IDC资产管理系统"`

### 2. 数据模型

- **User**（`models/user.py`）  
  - `email` 改为可空，支持 AD 域用户无邮箱  
  - 保留：`is_ad_user`、`ad_dn`、`display_name`
- **Asset**（`models/asset.py`）  
  - 新增/对齐文档 5.1 字段：  
    `asset_code`、`contract_number`、`purchased_with_server_sn`、`remark`、  
    `cpu_model`、`cpu_cores`、`memory_gb`、`disk_info`

### 3. Schema（Pydantic）

- **user.py**：`UserBase.email` 改为 `Optional[str] = None`
- **asset.py**：`AssetBase` / `AssetCreate` / `AssetUpdate` 增加上述资产字段；`AssetInDBBase` 增加 `deleted_at`

### 4. 路由

- **users.py**：注册时仅在有 `email` 时校验邮箱唯一；创建用户支持 `email` 为空

---

## 三、前端（与项目介绍 7.1 一致）

### 1. 组件（`frontend/src/components/`）

| 文档 7.1 组件 | 文件 | 说明 |
|---------------|------|------|
| AssetManagement.jsx | ✅ | 资产管理主页面 |
| AssetScrapManagement.jsx | ✅ | 资产报废管理 |
| PartsManagement.jsx | ✅ | 配件管理 |
| PartReceiptManagement.jsx | ✅ | 配件入库单管理 |
| ServerReceiptManagement.jsx | ✅ | 服务器入库单管理 |
| ServerDetail.jsx | ✅ | 服务器详情 |
| LoginForm.jsx | ✅ | 登录表单（支持 AD 域/本地） |
| UserManagement.jsx | ✅ | 用户管理 |
| ADConfigManagement.jsx | ✅ | AD 域配置管理 |

### 2. 菜单与流程

- **未登录**：仅显示登录页（LoginForm）
- **登录后**：主界面标题「IDC资产管理系统」，左侧菜单：
  - 资产管理
  - **报废管理**（待报废/已报废、确认报废）
  - 配件管理
  - **入库管理**（展开）
    - 配件入库单
    - 服务器入库单
  - 服务器管理（含机柜管理）
  - 部署新系统、监控数据
  - 用户管理、AD域配置

### 3. 页面标题

- **index.html**：`<title>IDC资产管理系统</title>`

---

## 四、启动与访问

### 后端

```bash
cd /root/idc资产管理/IDC项目/backend
python3 update_database_schema.py   # 首次或表结构变更后执行
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

- API 文档: http://localhost:8000/docs  
- 健康检查: http://localhost:8000/health  

### 前端

```bash
cd /root/idc资产管理/IDC项目/frontend
npm run start
# 或: npx vite --host 0.0.0.0
```

- 访问: http://localhost:3001  
- 首次打开为登录页，登录后进入主界面（含报废、入库等菜单）

---

## 五、与项目介绍的对齐项小结

- 技术架构：前后端分离，FastAPI + React 17 + Vite，SQLite，JWT + AD 域
- 数据表：users（含 AD 字段）、assets（含资产编码/合同/CPU/内存/磁盘等）、入库单/明细、服务器、AD 配置等
- 功能模块：资产管理（CRUD、状态、报废、回收站、Excel、历史）、入库单（服务器/配件）、报废管理、配件管理、服务器管理、用户管理、AD 域配置
- 前端：先登录再进系统，菜单含「报废管理」「入库管理」，标题为「IDC资产管理系统」
