# 后端日志分析报告

根据终端 890583 / 523509 中 uvicorn 输出整理。

---

## 一、问题汇总

### 1. 307 重定向导致 403（当前主要问题）

**现象：**
```
GET /api/v1/assets?status_filter=in_storage,in_use,maintenance → 307 Temporary Redirect
GET /api/v1/assets/?status_filter=... → 403 Forbidden
GET /api/v1/servers → 307
GET /api/v1/servers/ → 403 Forbidden
```

**原因：**  
- 路由注册为 `@router.get("/")`，完整路径为 `/api/v1/assets/`（带尾部斜杠）。  
- 前端请求的是 `/api/v1/assets?status_filter=...`（无尾部斜杠），FastAPI 返回 307 重定向到 `/api/v1/assets/?...`。  
- 浏览器或 axios 跟随重定向时，部分环境**不会**把 `Authorization` 头带到第二次请求，后端收到无 Token 请求 → 返回 403。

**结论：** 需要让前端请求**带尾部斜杠**，避免 307，从而避免 403。

---

### 2. 500：no such column: assets.remark

**现象：**
```
sqlite3.OperationalError: no such column: assets.remark
  File "./app/routes/assets.py", line 90, in get_pending_scrap_assets
```

**原因：** 当前进程使用的 SQLite 库里，`assets` 表没有 `remark` 列（可能用了未迁移的库或旧库）。

**处理：**  
- 对**实际在用的**数据库执行迁移：`python3 update_database_schema.py`（在 backend 目录下执行，确保改的是同一份 db）。  
- 若存在多份 db（如 `backend/server_management.db` 与 `backend/app/server_management.db`），需对**每一份**都跑一遍迁移，或统一只用一份。

---

### 3. 500：no such column: users.is_ad_user（另一终端/旧日志）

**现象：**
```
sqlite3.OperationalError: no such column: users.is_ad_user
  File "./app/utils/security.py", line 91, in get_current_user
```

**原因：** 某份数据库的 `users` 表缺少 AD 相关字段（如 `is_ad_user`）。

**处理：** 对**该**数据库执行同一迁移脚本，为 `users` 表补全字段。

---

### 4. 登录 401 后又能 200

**现象：**
```
POST /api/v1/users/login → 401 Unauthorized（若干次）
POST /api/v1/users/register → 200 OK
POST /api/v1/users/login → 200 OK
```

**原因：** 前几次为错误密码或用户不存在；注册或改用正确账号密码后登录成功。  
属正常业务逻辑，非后端故障。

---

## 二、已做/建议修复

1. **前端 API 地址**  
   - 资产、服务器等列表接口改为**带尾部斜杠**（例如 `/assets/`、`/servers/`），避免 307，防止 403。
2. **数据库迁移**  
   - 在 backend 目录执行：`python3 update_database_schema.py`。  
   - 确认 uvicorn 启动时的“当前工作目录”与迁移脚本使用的是**同一份** `server_management.db`；如有多个 db 文件，对每个都执行一次迁移或统一只用一个库。
3. **登录/注册**  
   - 登录失败时不再因 401 被全局拦截器清 token 并跳转，已排除 `/users/login`、`/users/register`，仅业务侧提示“用户名或密码错误”即可。

---

## 三、请求与状态码简要对应

| 状态码 | 含义 |
|--------|------|
| 200 OK | 成功（登录、注册、部分资产/服务器请求） |
| 307 Temporary Redirect | 路径无尾部斜杠被重定向 |
| 401 Unauthorized | 登录失败（用户名/密码错误）或 Token 无效 |
| 403 Forbidden | 未带 Token（多为 307 后重定向请求未带 Authorization） |
| 500 Internal Server Error | 数据库缺列（如 assets.remark、users.is_ad_user） |
