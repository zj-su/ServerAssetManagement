# IDC项目快速启动指南

**更新时间**: 2025-01-27

---

## 🚀 快速启动步骤

### 1. 安装依赖

```bash
cd /root/idc资产管理/IDC项目/backend
pip3 install -r requirements.txt
```

### 2. 重启后端服务

```bash
# 停止旧服务
pkill -f "uvicorn app.main:app"

# 启动新服务
cd /root/idc资产管理/IDC项目/backend
nohup python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &

# 等待3秒后查看日志
sleep 3
tail -20 /tmp/backend.log
```

### 3. 验证服务

```bash
# 检查后端服务
curl http://localhost:8000/api/v1/health

# 检查进程
ps aux | grep uvicorn
```

### 4. 测试功能

#### 4.1 测试登录
```bash
# 测试本地用户登录
curl -X POST "http://localhost:8000/api/v1/users/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

#### 4.2 测试新API
```bash
# 获取AD域配置（需要Token）
TOKEN="your_token_here"
curl -X GET "http://localhost:8000/api/v1/ad-config/" \
  -H "Authorization: Bearer $TOKEN"

# 获取入库单列表（需要Token）
curl -X GET "http://localhost:8000/api/v1/receipts/" \
  -H "Authorization: Bearer $TOKEN"
```

---

## 📋 功能验证清单

### 后端功能
- [ ] 后端服务正常启动
- [ ] 登录API正常（本地用户）
- [ ] 用户管理API正常
- [ ] 入库单API正常
- [ ] AD域配置API正常
- [ ] 资产API权限控制正常

### 前端功能
- [ ] 登录界面正常显示
- [ ] 登录功能正常
- [ ] 用户管理界面正常
- [ ] AD域配置界面正常
- [ ] Token自动携带正常
- [ ] 401错误自动处理正常

---

## 🔧 常见问题

### 问题1：依赖安装失败
**解决**：检查Python版本（建议3.8+），使用pip3安装

### 问题2：服务启动失败
**解决**：检查端口8000是否被占用，查看日志文件

### 问题3：数据库错误
**解决**：删除旧数据库文件，让系统自动创建新表

### 问题4：AD域功能不可用
**解决**：这是正常的，需要先配置AD域服务器信息

---

## 📝 注意事项

1. **首次使用**：需要创建admin账户（如果不存在）
2. **AD域功能**：需要先配置AD域服务器才能使用
3. **权限控制**：所有修改操作都需要登录
4. **Token过期**：Token有效期8天，过期后需要重新登录

---

**修复完成！可以开始使用了！** 🎉
